# =============================================================================
# SINGLE SOURCE OF TRUTH FOR DOCKER: ../../MC Server/docker-compose.yml
#
# That stack runs jebaited-mc (Paper on :25565), PostgreSQL, MySQL, web-admin,
# Redis, and the Discord bot. Do NOT run a second Paper or second Postgres from
# this repo — it duplicated ports and broke everything.
#
# From repo root:
#   cd "..\..\..\MC Server"
#   docker compose up -d --build
#
# Deploy this plugin JAR into MC Server's bind-mounted data:
#   MC Server/plugins/JebaitedCore.jar
#   (or Vibe Code\scripts\build-jebaitedcore-docker.ps1 with MC Server plugins path)
# =============================================================================
include:
  - path: "../../../JebaitedNetwork/docker-compose.yml"
